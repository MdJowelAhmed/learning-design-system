export { ThemeProvider, useTheme } from './ThemeProvider';
export type {
  Theme,
  ResolvedTheme,
  ThemeContextValue,
  ThemeProviderProps,
} from './ThemeProvider';

export { themeScript, getThemeScriptTag } from './theme-script';
